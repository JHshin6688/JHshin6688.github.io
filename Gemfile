source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'
gem 'connection_pool', '2.5.0'

# Ruby 3.4+ removed these from the standard library; github-pages pins an
# older Jekyll that still expects them to be implicitly available.
gem 'csv'
gem 'logger'
gem 'base64'
gem 'bigdecimal'
